# Production build-time variables for Vite.
# On Vercel, set these in Project → Settings → Environment Variables
# (they override this file during `vercel build`).

VITE_API_URL=https://YOUR-BACKEND-NAME.onrender.com/api
VITE_ENV=production
